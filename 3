package main

import "fmt"

func main() {
	map1 := map[int]int{
		1: 2,
		2: 4,
		3: 6,
	}

	fmt.Println(Map(map1))
}

func Map(map1 map[int]int) int {
	map2 := 0

	for _, y := range map1 {
		map2 += y
	}

	return map2
}

package main

import (
	"fmt"
	"strings"
)

func frequenciaLetras(s string) map[string]int {
	map1 := make(map[string]int)
	char := strings.Split(s, " ")

	for _, ranC := range char {
		map1[ranC] += 1
	}
	return map1
}

func main() {
	fmt.Println(frequenciaLetras("Hoje sou muito "))
}
